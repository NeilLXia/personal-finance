# Vite dev proxy target
API_HOST=http://127.0.0.1:8001

# Cloudflare Pages Function or Worker proxy target for /api/* in deployed environments.
# Include the backend port when the Node app is exposed directly.
BACKEND_ORIGIN=http://127.0.0.1:8001
